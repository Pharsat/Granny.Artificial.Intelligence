
class Prefixes:
    header = 'header'
    body = 'body'
    subject = 'subject'


class Suffixes:
    max_length = '_max_length.pickle'
    metadata = '_meta.tsv'
    model = '_model.h5'
    tokenizer = '_tokenizer.pickle'
    vectors = '_vecs.tsv'
